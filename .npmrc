# Use exact versions for dependencies
save-exact=true

# Faster npm installs
prefer-offline=true
audit=false
fund=false

# Use npm registry (ensures consistency)
registry=https://registry.npmjs.org/

# Increase network timeout for Railway builds
fetch-timeout=300000
fetch-retries=5
